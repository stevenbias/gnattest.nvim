# Neovim versions for testing matrix
versions:
  stable: "stable"
  nightly: "nightly"
  # Add future versions here
  # v0.10: "v0.10.0"

# Default version for single-version workflows
default: "stable"

# Version for coverage reporting
coverage: "stable"